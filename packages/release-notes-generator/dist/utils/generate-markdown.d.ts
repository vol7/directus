import type { Notice, PackageVersion, Type, UntypedPackage } from '../types.js';
export declare function generateMarkdown(notices: Notice[], types: Type[], untypedPackages: UntypedPackage[], packageVersions: PackageVersion[]): string;
